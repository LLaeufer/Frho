fun one () (1);
fun count (a: INT, prev: FUNCTION () (INT)) (if (a == 10) then (prev) else (
    fun plus_one(with_fun: FUNCTION () (INT)) (1 + with_fun());
    fun combine() (plus_one(prev));
    count(a+1, combine)
));
let func = count(0, one);
func()